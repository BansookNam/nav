name: nav_hooks
description: Provide easy way to navigate. Includes lots of routers. You can use this library on Android & iOS.
version: 1.4.1
homepage: https://github.com/bansooknam/nav
#dart format .

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_hooks: ^0.20.0
  meta: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: